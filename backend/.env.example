# Example environment for backend
LOG_LEVEL=INFO
# Comma-separated exchanges to enable. Leave empty for defaults
# (binance, bybit, mexc, bingx, gate). Prefix with '-' to exclude defaults,
# e.g. "-gate" keeps all except Gate; "binance,-mexc" keeps defaults and adds
# Binance explicitly while disabling MEXC.
ENABLED_EXCHANGES=
# Explicit per-connector toggles. Set to 0/false to disable, 1/true to enable.
# Examples:
# CONNECTOR_BINANCE=1
# CONNECTOR_GATE=0
# Request timeouts (seconds)
HTTP_TIMEOUT=10
WS_CONNECT_TIMEOUT=10
