# Example environment for backend
LOG_LEVEL=INFO
# Comma-separated exchanges to enable, e.g. "binance,bybit,okx"
ENABLED_EXCHANGES=binance,bybit
# Request timeouts (seconds)
HTTP_TIMEOUT=10
WS_CONNECT_TIMEOUT=10
